import { SingleProductReducerState } from '@/types';

export const initialState: SingleProductReducerState = {
  data: undefined,
  error: null,
  isLoading: true,
};

export const initialAction = () => {};
